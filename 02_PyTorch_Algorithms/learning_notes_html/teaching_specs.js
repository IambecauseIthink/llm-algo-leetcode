// Authored teaching plans. Each lesson has a concrete story, a worked example,
// a failure case and a matching experiment; none is copied from a local notebook.
const plans = {};
function add(id, hook, analogy, terms, steps, trap, code, lab, check) {
  plans[id] = {hook, analogy, terms, steps, trap, code, lab, check};
}
add(30, '一本书放不进书包，换大书包就一定是好办法吗？',
 '把上下文窗口想成书包：prompt 是教材，response 是你要写的笔记。先给笔记留位置，再决定能装多少教材。长度预算解决“装得下”；数据和评测解决“装进去有没有用”。',
 [['token','模型处理的文字片段，不等于一个汉字。'],['窗口','一次输入与生成可使用的总 token 预算。'],['packing','把较短样本组织到批次中，减少空位；仍要处理样本边界。']],
 [['留出回答空间','窗口 16 格，给回答留 4 格，prompt 最多占 12 格。一个 13 token 的 prompt 就超了 1 格。'],['观察分布','样本长度是 4、8、12、15：12 格预算能放下前三个，fit rate 是 3/4，而不是看最大样本 15 就断言全部失败。'],['理解代价','普通 dense Attention 的分数元素数按长度平方增长：长度 8→16，64→256。这里比较元素数量，不预测真实耗时。']],
 '把字符长度当 token 长度，或把 fit rate 上升当质量提升。扩大窗口后仍要用目标 tokenizer 和同一评测集复核。',
 'window, response = 16, 4\nlengths = [4, 8, 12, 15]\nbudget = window - response\nfits = [n <= budget for n in lengths]\nprint(sum(fits) / len(fits))  # 0.75', 'context',
 ['窗口 16，回答预留 4，prompt 长 13。还差多少空间？','1 token',['已经能放下','4 token'],'可用 prompt 预算是 16−4=12，13−12=1。']);
add(31, '两张很小的补丁，为什么能改变一个巨大的矩阵？',
 'LoRA 像给固定机器装可调节的小配件。原权重 W 保留，一条 A→B 旁路学习修正量。rank 是这条旁路的中间宽度，不是最终模型的层数。',
 [['rank r','低秩旁路的中间维度，决定可训练参数规模。'],['alpha/r','常见 LoRA 缩放方式；不同变体可能采用不同缩放。'],['target modules','把旁路装在哪些线性层，决定覆盖范围。']],
 [['数完整矩阵','8 个输入映射到 6 个输出，完整权重需要 8×6=48 个数。'],['数两块补丁','rank=2：A 有 2×8=16 个数，B 有 6×2=12 个数，共 28 个。'],['看收益边界','rank=4 时旁路要 56 个数，已超过这个小矩阵的 48 个。低秩只有在 r 足够小时才省；实际大模型维度更大。']],
 '只比较 rank。目标层、缩放、dropout、数据与训练步数都会改变结果，变体应在统一设置下比较。',
 'd_in, d_out, rank = 8, 6, 2\nfull = d_in * d_out\nadapter = rank * (d_in + d_out)\nprint(full, adapter)  # 48, 28', 'lora',
 ['8→6 的线性层，rank=2 的 A、B 一共多少参数？','28',['48','16'],'2×8+6×2=28，别只数 A。']);
add(32, '四条看起来正常的数据，为什么只有两条适合训练？',
 '先把数据当作食材：菜谱再好，也救不了空食材和重复配送。SFT 数据工程先统一字段，再检查内容，最后才把文本变成模型输入。',
 [['record','一条原始记录，字段名和内容可能不规范。'],['duplicate','重复样本；要先定义按哪个字段组合去重。'],['labels mask','不参与监督的位置设为忽略值，通常是 -100。']],
 [['统一外形','把 question/answer 映射到 prompt/response，并清掉首尾空白，避免同一含义出现多种字段。'],['检查四条记录','A 正常，B 的回答只有空格，C 与 A 完全重复，D 正常。按非空、精确去重规则，保留 A、D，共 2/4。'],['再做 token 检查','字符清洗不能代替 tokenizer。最终还要检查 chat template、截断位置以及 response 是否仍有监督 token。']],
 '先用原始字符串去重，再清理空格，会漏掉“内容相同但空格不同”的重复。还要避免训练与评测集合泄漏。',
 'pairs = [("问", "答"), ("问2", " "), ("问", "答"), ("问3", "答3")]\nclean = {(p.strip(), r.strip()) for p, r in pairs if p.strip() and r.strip()}\nprint(len(clean))  # 2', 'data',
 ['四条数据中一条空回答、一条与正常记录重复，保留几条？','2 条',['3 条','4 条'],'空回答剔除，重复的第二份也剔除。']);
add(33, '发动机能点火，就能发车去远方了吗？',
 '微调 readiness 像出发前检查：知道目的地、有足够油、带了地图，才值得启程。脚本能启动只是其中一项。',
 [['计划','明确模型、数据、训练方法和要验证的假设。'],['预算','时间与显存上限，需要可检查的数值。'],['评测目标','训练结束后凭什么判断这次尝试有效。']],
 [['列输入','先写模型、有效数据量、训练步数、batch、评测集；缺一个就先记为待补，别默默填默认值。'],['算预算','100 步，每步按 2 秒估算，需要 200 秒；只有 150 秒预算时计划不满足约束。'],['决定下一步','可以缩小试验验证流程，或调整预算。缩小后的 smoke test 不能冒充原计划的完整评测。']],
 '把“数据存在”当成“数据可用”，把“能加载”当成“可训练且可评测”。检查应能说清楚缺了哪一项。',
 'steps, seconds_per_step, budget_s = 100, 2, 150\nrequired_s = steps * seconds_per_step\nprint(required_s <= budget_s)  # False', 'gate',
 ['100 步×2 秒/步，预算 150 秒，计划是否满足时间约束？','不满足，预计超出 50 秒',['满足，只有 100 步','需要先删掉评测'],'200−150=50 秒；步数和秒数不能直接比较。']);
add(34, '三位顾客念同一段开场白，服务员需要每次都重听吗？',
 'Prefix Cache 像保存已经读过的开场白。但复用必须从开头连续相同；只有中间几个词相同不算前缀。Chunked Prefill 则把还没处理的后半段分批读完。',
 [['prefix hit','从第一个 token 起连续命中的长度。'],['suffix','未命中的后缀，仍需要 prefill。'],['chunk','将剩余工作切成大小有限的小块。']],
 [['先比开头','缓存是 A B C D，请求是 A B C X Y，连续命中 3 个，X 处停止。'],['只处理剩余','5 个 token 减去 3 个命中，suffix 为 X Y，共 2 个。'],['再切块','另一请求还剩 7 个 token，chunk size=3 时分成 3、3、1。它们合计仍是 7，分块没有抹掉计算。']],
 '先对完整 prompt 切块再声称节省了所有 prefill。要先确定可复用部分，再规划未命中的 suffix。',
 'prompt, hit, chunk = 10, 4, 3\nsuffix = prompt - hit\nchunks = [min(chunk, suffix-i) for i in range(0, suffix, chunk)]\nprint(chunks)  # [3, 3]', 'prefix',
 ['prompt 10，前缀命中 4，chunk=3，需要几块新 prefill？','2 块',['4 块','0 块'],'剩余 6 个，分为 3+3。']);
add(35, '草稿一次写四个字，为什么不能跳过错误继续抄？',
 '草稿模型像速记员，目标模型像审稿人。后面的候选依赖前面的上下文，所以第一个错误会让后续草稿失去原先的条件。',
 [['proposal','草稿提出的一串候选 token。'],['verify','目标模型按规则验证候选。'],['accepted prefix','从开头连续通过的部分。']],
 [['提出草稿','本轮提议 4 个 token。假设课堂验证结果依次为 对、对、错、对。'],['在首错处停止','只能接受前 2 个，不能把第 4 个“对”捡回来，因为它依赖未通过的第 3 个。'],['记录推进和成本','回退规则还可能增加目标 token；实际实现需要记录 accepted、fallback 和验证成本，不能把候选数等同推进数。']],
 '数一串布尔值里 True 的总数当接受长度。连续前缀是 2，True 总数 3 是另一件事。',
 'checks = [True, True, False, True]\naccepted = 0\nfor ok in checks:\n    if not ok: break\n    accepted += 1\nprint(accepted)  # 2', 'speculate',
 ['验证结果为 对、对、错、对，连续接受多少？','2',['3','4'],'遇到第一次拒绝立即停，后面的结果不能独立复用。']);
add(36, '一家只有一位厨师的店，应该先做哪份订单？',
 '调度器每天都在回答“下一步服务谁”。排队顺序影响等待；分批执行又影响资源利用。先理解简单 FIFO，再研究更复杂的 phase、cache 和 priority 规则。',
 [['queue','已经到达但尚未执行的请求。'],['policy','决定从队列选谁的规则。'],['waiting time','开始服务时刻减去到达时刻。']],
 [['固定工作量','三个请求同时到达，服务时长是 6、2、1。先暂时假设单服务台、不抢占。'],['按先来顺序','FIFO 等待分别为 0、6、8，平均 14/3≈4.67。'],['按短任务优先','顺序变成 1、2、6，等待 0、1、3，平均 4/3≈1.33；总服务量仍为 9，长请求公平性需要另外考虑。']],
 '把教学例子的短任务优先当作所有在线系统的最佳策略。未来任务长度可能未知，还要处理饥饿和业务优先级。',
 'durations = [6, 2, 1]\nclock, waits = 0, []\nfor duration in durations:\n    waits.append(clock)\n    clock += duration\nprint(waits)  # [0, 6, 8]', 'schedule',
 ['三个请求同时到达，FIFO 服务时长 6、2、1，第三个等待多久？','8 个时间单位',['9 个时间单位','1 个时间单位'],'等待是前两个的服务时间 6+2，不包括自己的执行。']);
add(37, '书架满了，应该丢掉最厚的书，还是最久没看的书？',
 'KV Cache 调度是在容量受限时决定保留什么。不同驱逐策略衡量不同的“价值”，不能只用一个名字判断好坏。',
 [['capacity','可保存的缓存容量。'],['LRU','优先淘汰最长时间未访问的条目。'],['stale entry','优先级已过期的队列记录，不能按旧值做决策。']],
 [['登记访问','书架容量 2，访问顺序 A、B、A，此时 A 最新，B 最旧。'],['新书到来','访问 C 时要腾位置。LRU 驱逐 B，留下 A、C，而不是因为 A 最先入架就驱逐 A。'],['更新队列','若用堆维护优先级，A 更新后旧条目可能还在堆中，取出时要核对版本或当前状态。']],
 '把最近插入当最近访问，或者在条目更新后仍使用旧优先级。Notebook 的评分规则也可能不是纯 LRU。',
 'from collections import OrderedDict\ncache = OrderedDict()\nfor key in ["A", "B", "A", "C"]:\n    cache[key] = True\n    cache.move_to_end(key)\n    if len(cache) > 2: cache.popitem(last=False)\nprint(list(cache))  # A, C', 'cache',
 ['容量 2，访问 A、B、A、C，LRU 淘汰谁？','B',['A','C'],'A 刚访问过，B 才是最久未访问的条目。']);
add(38, '读菜单与做菜速度不同，分成两条生产线就一定更快吗？',
 'Prefill 处理输入，Decode 逐步生成。拆成两个池像把备菜和出菜分工，但两边速度不匹配会产生新队列，状态交接也有成本。',
 [['prefill','处理输入 token 并建立缓存状态。'],['decode','利用历史缓存逐 token 生成。'],['disaggregation','把两个阶段安排到不同资源池。']],
 [['看两段能力','一个简化流水线每秒能 prefill 8 个请求、decode 4 个请求，稳定完成率最多是每秒 4 个。'],['加错地方','把 prefill 从 8 加到 16，decode 仍是 4，瓶颈没有移动。'],['算交接成本','还要搬运 KV 状态并排队。容量公式只给出理想上界，不等于真实延迟。']],
 '看到拆池后某一阶段更快就宣布端到端更快。要看瓶颈池、交接和同一请求分布。',
 'prefill_capacity, decode_capacity = 8, 4\nideal_capacity = min(prefill_capacity, decode_capacity)\nprint(ideal_capacity)  # 4 requests/s (ideal bound)', 'pipeline',
 ['理想两段能力为 8 与 4 请求/秒，完成率上限是多少？','4 请求/秒',['8 请求/秒','12 请求/秒'],'每个请求都要过两段，较慢的一段限制整条流水线。']);
add(39, '电量只剩 10%，手机应该先关掉什么？',
 '推理回退也要分层：高优先级请求、普通请求、可延后请求的服务要求不同。回退规则要明确“允许牺牲什么”和“不能牺牲什么”。',
 [['tier','按服务要求划分的请求层级。'],['fallback','主路径不能满足条件时采用的备选路径。'],['hard constraint','即使压力很大也不能跨过的门槛。']],
 [['列条件','候选 A 更快但质量 0.85；候选 B 慢一些但质量 0.94。需求规定质量至少 0.90。'],['先排不可行','A 已违反质量门槛，不能靠速度加分重新进入可行集合。'],['再做分层','若 B 资源不足，可以排队或拒绝部分低优先级请求；具体动作应由服务契约决定。']],
 '把所有指标加权求一个分数，让速度补偿不可接受的质量损失。硬门槛应先过滤。',
 'candidates = [{"name":"A","quality":.85}, {"name":"B","quality":.94}]\nfeasible = [c for c in candidates if c["quality"] >= .90]\nprint([c["name"] for c in feasible])  # B', 'gate',
 ['质量下限 0.90，最快候选质量 0.85，可以直接选吗？','不能，它违反硬约束',['可以，速度能补偿','把下限改成 0.85 就行'],'改变门槛会改变问题，应按原有服务要求判断。']);
add(40, '把尺子刻度变粗，为什么有的误差特别伤输出？',
 '权重量化像用更粗的尺子记录长度。GPTQ 和 AWQ 都关注低比特误差，但不是同一种算法：GPTQ 利用近似二阶信息补偿，AWQ 利用激活信息寻找重要通道。',
 [['scale','一个整数刻度对应多少浮点数。'],['group','共享量化参数的一组权重。'],['calibration','用代表性输入观察分布或敏感性。']],
 [['先学最小闭环','scale=0.5 时，0.9/0.5=1.8，四舍五入为 2，恢复为 1.0，误差为 0.1。'],['比较影响','y=xw。若 x=1，权重误差 0.1 造成输出误差 0.1；若 x=10，输出误差变成 1.0。'],['理解校准','所以同样的权重误差对不同通道的后果不同。下面的对称量化实验只解释机制，不实现完整 GPTQ/AWQ。']],
 '仅按权重绝对误差排名就等同 AWQ，或把分组对称量化称作完整 GPTQ。校准分布必须与使用场景相关。',
 'weight, scale = 0.9, 0.5\nq = round(weight / scale)\nrestored = q * scale\nprint(q, restored)  # 2, 1.0', 'quant',
 ['权重误差为 0.1，输入激活为 10，线性输出误差是多少？','1.0',['0.1','0.01'],'Δy=x×Δw=10×0.1=1。']);
add(41, '书变薄了，为什么随着对话变长，书包还是越来越重？',
 '模型权重是固定书本，KV Cache 是每轮增加的笔记。压缩书本与压缩笔记解决不同的问题；越长的对话越要看缓存账本。',
 [['FP8','8 位浮点格式族，不是简单的 INT8 改名。'],['KV Cache','为后续生成保留的 Key/Value 状态。'],['dtype bytes','每个元素占用字节数，例如 FP16 通常为 2。']],
 [['数元素','单层、1 个 KV head、维度 4、序列长 8：K 和 V 合计 2×1×4×8=64 个元素。'],['换存储精度','每元素 2 字节时占 128 字节，1 字节时理论数据占 64 字节，暂不计 scale 和元数据。'],['检查误差','低精度引入舍入/范围限制，不能只算字节数，还要比较恢复误差和生成质量。']],
 '把对称整数玩具量化当作硬件 FP8 实现，或以为 KV 字节减半就让整个服务快两倍。',
 'tokens, kv_heads, dim = 8, 1, 4\nelements = 2 * tokens * kv_heads * dim\nprint(elements * 2, elements * 1)  # 128, 64 bytes', 'kv',
 ['同样 64 个缓存元素，从 2 字节改成 1 字节，纯数据区节省多少？','64 字节',['128 字节','32 字节'],'128−64=64，尚未算 scale/元数据。']);
add(42, '把东西搬到仓库能腾出房间，但取回来要多久？',
 'Activation offload 将反向传播稍后需要的激活暂存到主机内存。房间是 GPU，仓库是 CPU；腾空间的代价是往返搬运。',
 [['activation','前向计算产生、反向可能需要的中间状态。'],['offload','把状态暂时移到另一层存储。'],['checkpoint','少保存状态，需要时重算；与搬运是不同路径。']],
 [['先算腾多少','激活 12 GiB，保留 4 GiB，搬走 8 GiB，则这部分驻留由 12 降到 4。'],['再算往返','假设有效带宽 8 GiB/s，8 GiB 去程 1 秒、回程 1 秒，总共 2 秒，未计重叠。'],['与重算比较','若重算需要 1.5 秒，在这个简化条件下重算时间更少；实际需要比较可重叠区间和峰值。']],
 '只算去程或把链路标称带宽当作有效带宽。搬运可以重叠，但不是默认完全免费。',
 'moved_gib, bandwidth_gib_s = 8, 8\nroundtrip_s = 2 * moved_gib / bandwidth_gib_s\nprint(roundtrip_s)  # 2.0', 'offload',
 ['搬走 8 GiB、有效带宽 8 GiB/s，再搬回来，串行搬运多久？','2 秒',['1 秒','0 秒'],'去、回各一次：2×8/8=2。']);
add(43, '房租够付，为什么月底还是没钱？',
 '显存管理需要把固定开销、随请求增长的开销和临时开销放在同一本账上。只看权重会漏掉激活、缓存与工作区。',
 [['resident','当前仍占据设备内存的对象。'],['working set','当前这段计算实际需要访问的状态。'],['peak','同一时刻的总占用最高值。']],
 [['分项记账','权重 6、缓存 3、临时工作区 2，总共 11 GiB。不能看到权重只有 6 就说 10 GiB 的卡足够。'],['看生命周期','若两块临时区从不同时存活，可复用空间；如果同时存活必须相加。'],['决定迁移','优先知道对象什么时候再使用，再比较迁移开销，避免搬走后马上取回。']],
 '把每个对象各自历史最大值直接相加当精确峰值，或者只看进程结束时的占用。需要时间轴。',
 'ledger = {"weights":6, "cache":3, "workspace":2}\nprint(sum(ledger.values()))  # 11 GiB if simultaneous', 'memory',
 ['三项同时占用 6、3、2 GiB，10 GiB 预算还差多少？','1 GiB',['4 GiB','已经够用'],'总计 11，超过预算 1。']);
add(44, '调参像选套餐：最贵的菜一定最适合你吗？',
 '自动调优先定义菜单和预算，再尝试候选。搜索空间越大不代表结论越好；测量噪声、质量门槛和验证成本都要考虑。',
 [['configuration','一组具体参数，如 batch、dtype、chunk size。'],['feasible','同时满足硬约束的候选。'],['objective','在可行候选中要最大化或最小化的指标。']],
 [['写目标','预算 10 GiB、质量至少 0.90，在此条件下追求吞吐。'],['过滤','A: 12 GiB/0.95/120 tokens/s，B: 8 GiB/0.93/100，C: 6 GiB/0.85/150。只有 B 可行。'],['再验证','对 B 重复同输入测量。单次偶然快不意味着稳定领先，真实硬件测量还要控制 warm-up。']],
 '先选最高吞吐，再试图解释为什么超预算也能接受。搜索规则本身需要明确定义。',
 'runs = [(12,.95,120),(8,.93,100),(6,.85,150)]\nvalid = [r for r in runs if r[0] <= 10 and r[1] >= .90]\nprint(max(valid, key=lambda r:r[2]))', 'gate',
 ['A 超显存，C 不达质量，B 满足两项。谁进入吞吐排名？','只有 B',['A 和 B','只选吞吐最高的 C'],'先过滤硬约束，再优化目标。']);
add(45, '箱子超重了，先拿出最重的物品就够了吗？',
 '显存裁剪要找到真正的峰值来源，再选可行策略。参数、激活和缓存分别适合不同手段，不能看到 OOM 就一律量化。',
 [['bottleneck','当前约束中最先限制方案的部分。'],['saving','相对同口径 baseline 减少的占用。'],['trade-off','节省资源同时引入的时间、质量或复杂度代价。']],
 [['定位峰值','权重 4、激活 10、其他 2，总共 16 GiB，预算 12。'],['尝试方案','若仅权重减半，节省 2，还剩 14，不够；若激活降到 6，总计恰好 12。'],['留安全余量','恰好 12 只是课堂预算通过。真实分配器、动态输入和临时工作区会带来额外波动。']],
 '把削减比例最大的对象当作最有效方案。要比较绝对节省量，并检查是否真的跨过预算门槛。',
 'weights, activations, other = 4, 10, 2\nprint(weights/2 + activations + other)  # 14\nprint(weights + 6 + other)  # 12', 'memory',
 ['4+10+2 GiB 的账本，仅权重减半后总量是多少？','14 GiB',['8 GiB','12 GiB'],'只有权重从 4 降为 2，其他项不变。']);
add(46, '两个同学同时干活，为什么总时间不能直接相加？',
 '通信 trace 像两条时间轨道：计算与传输可能同时发生。要用区间判断重叠，而不是看到两段都存在就认定实现了 overlap。',
 [['collective','多设备共同参与的通信操作，如 AllReduce。'],['overlap','两个事件在时间轴上同时进行的部分。'],['critical path','决定最早完成时刻的依赖链。']],
 [['画区间','计算在 [0,6]，通信在 [4,8]，共同覆盖 [4,6]，重叠 2。'],['算覆盖时间','6+4−2=8，不能把总时长说成 10。'],['读等待','即使某次通信短，频繁同步也可能让关键路径变长；记录起止时刻和依赖才能解释。']],
 '把两个事件名出现在同一 trace 当成已经重叠，或用所有 overlap 之和直接推出端到端加速。',
 'compute, comm = (0,6), (4,8)\noverlap = max(0, min(compute[1],comm[1])-max(compute[0],comm[0]))\nprint(overlap)  # 2', 'overlap',
 ['计算 [0,6]，通信 [4,8]，重叠长度是多少？','2',['4','6'],'交集是 [4,6]。']);
add(47, '四个窗口都开着，为什么还是有人排长队？',
 'MoE 专家像不同专科窗口：Router 决定 token 去哪里，专家并行再把专家放到不同设备。窗口数量多并不能保证任务分得均匀。',
 [['expert','处理部分 token 的子网络。'],['dispatch','把 token 按路由结果发往对应专家。'],['all-to-all','多个设备彼此交换各自目的地的数据。']],
 [['先数路由','8 个 token 分给两个专家，负载是 6 和 2。'],['找关键路径','假设两个专家同速，完成时间由较忙的 6 决定；总工作量/设备数=4 是理想均分值。'],['看通信','如果每个 token 都需要跨设备发送，还会增加传输和等待。必须一起记录负载和通信量。']],
 '把 expert 数当作有效并行度，或只看平均负载而忽略最忙设备。',
 'loads = [6, 2]\nideal = sum(loads) / len(loads)\nefficiency = sum(loads) / (max(loads) * len(loads))\nprint(ideal, round(efficiency,2))  # 4, 0.67', 'parallel',
 ['两个同速专家处理 6 与 2 份工作，理想并行完成时间由谁决定？','6 份工作的专家',['平均 4 份','2 份工作的专家'],'其他专家完成后要等待最长的那一段。']);
add(48, '是路太窄，还是车在路口等得太久？',
 '通信慢可能是数据多，也可能是等待别人到齐。把“传输时间”和“同步等待”分开，才能选择压缩、拓扑调整或负载均衡。',
 [['wait','通信真正开始前或同步点上的等待。'],['transfer','数据实际传输的时间。'],['hotspot','对关键路径贡献显著的事件。']],
 [['拆开总量','事件 A: 传输 2ms、等待 8ms；事件 B: 传输 5ms、等待 1ms。'],['选优先级','A 总计 10ms 更长，但其主要成本是等待。只把带宽加倍，传输从 2 到 1，总计仍是 9。'],['找原因','检查 rank 到达时间、工作分配和同步位置，再决定是否需要带宽优化。']],
 '把所有等待都记为网络传输，从而把负载不均衡误判成带宽不足。',
 'transfer_ms, wait_ms = 2, 8\nprint(transfer_ms + wait_ms)  # 10\nprint(transfer_ms/2 + wait_ms)  # 9', 'overlap',
 ['传输 2ms、等待 8ms，传输时间减半后总计多少？','9ms',['5ms','1ms'],'等待 8ms 没有变化，所以是 1+8。']);
add(49, '合租房子、拆开家具、分工装修：它们解决同一种问题吗？',
 '并行策略切分的对象不同：DP 分数据，TP 分单层计算，PP 分层，ZeRO 分训练状态。先说明放不下什么、慢在哪里，再选工具。',
 [['DP','模型副本分别处理不同数据，再同步梯度。'],['TP','把一个层的张量计算拆到多设备。'],['PP','按层切成阶段，用 microbatch 填充流水线。']],
 [['识别对象','如果单个矩阵就放不进一张卡，仅把不同数据发到多个完整副本没有帮助。'],['形成候选','可以研究 TP 切矩阵；若主要是 optimizer state 大，可研究 ZeRO 的状态分片。'],['加入约束','切分会引入通信。相同切法在高速互联与慢速网络上的结果可能不同。']],
 '把 DP、TP、PP 的缩写当作速度排名。它们是切分策略，收益依赖瓶颈、硬件和 workload。',
 'problem = "single_layer_too_large"\nnext_candidate = {"single_layer_too_large":"tensor_parallel",\n                  "optimizer_state_too_large":"state_sharding"}\nprint(next_candidate[problem])', 'parallel',
 ['单个层的矩阵放不进单卡，哪种方法直接切这个层？','Tensor Parallelism',['仅增加数据并行副本','仅减小评测集'],'TP 切分层内张量计算；DP 仍保留完整模型副本。']);
add(50, '投票赢了 8 次，就代表胜率 80% 吗？',
 '偏好评测像投票，先确定哪些票有效，平票怎么处理，再算胜率。分母含义不一致时，两个百分数不能直接比较。',
 [['preference pair','同一输入下 chosen 与 rejected 的配对。'],['win rate','按约定口径统计的胜利比例。'],['coverage','评测覆盖了多少有效样本或场景。']],
 [['先查合法性','同一条 pair 的两个回答相同，或者其中一个为空，会削弱偏好信号。'],['固定口径','12 条评测，8 胜、2 负、2 无效。若排除无效，胜率=8/10=80%；若除以全部，则是 66.7%。'],['同时报告覆盖','80% 应配上有效 10/12，让读者知道剩下两条没有被评到。']],
 '只报告胜率不报告分母；或在 baseline 与 candidate 中用不同规则处理无效样本。',
 'wins, losses, invalid = 8, 2, 2\nwin_rate = wins / (wins + losses)\ncoverage = (wins + losses) / (wins + losses + invalid)\nprint(win_rate, coverage)', 'preference',
 ['8 胜、2 负、2 无效，按有效样本计算的胜率是多少？','80%',['66.7%','100%'],'有效样本数是 10，分母不包含那两条无效记录。']);
add(51, '每天听新意见就改菜单，会不会越改越乱？',
 'Online DPO 把新反馈持续纳入更新。反馈会改变采样分布，模型更新又影响未来反馈，因此需要版本、窗口和稳定评测。',
 [['feedback stream','随时间到来的偏好反馈。'],['update window','每次更新采用的时间范围或样本集合。'],['reference','用于比较的固定或按协议更新的参照。']],
 [['留住 baseline','先在固定评测集上记录离线方案的胜率和更新成本。'],['比较窗口','候选胜率 0.60 对 baseline 0.55，提升 5 个百分点；若更新耗时从 1s 到 4s，也要记录。'],['检查稳定性','多个窗口中偶尔一次高分不能代表持续收益，场景分布变化也可能制造假象。']],
 '把来自不同用户群的两次胜率当作模型提升；或把在线更新等同每来一条反馈就立即训练。',
 'baseline, candidate = .55, .60\nprint((candidate-baseline)*100)  # 5 percentage points', 'preference',
 ['胜率从 55% 到 60%，提升多少个百分点？','5 个百分点',['5% 相对增幅','60 个百分点'],'百分点是两个百分数之差；相对增幅是 5/55≈9.1%。']);
add(52, '考试总分更高，就可以忽略某门课不及格吗？',
 '对齐指标像一张成绩单：偏好、安全、格式、任务完成度可能相互冲突。先定义及格线，再讨论综合表现。',
 [['metric conflict','一个指标改善、另一个重要指标变差。'],['threshold','能接受的最低值或最高值。'],['Pareto','在所有目标上不更差且至少一项更好的比较关系。']],
 [['看单项','A 胜率 0.70、安全 0.85；B 胜率 0.65、安全 0.96。'],['应用门槛','若安全至少 0.95，只有 B 可行。不能把 A 较高胜率直接抵消安全不达标。'],['说明取舍','若二者均满足门槛但各有优势，应说明目标优先级，而不是假装存在唯一最优。']],
 '为了让候选通过，事后修改阈值；这会让评测失去约束意义。',
 'safety_floor = .95\nruns = [("A", .70, .85), ("B", .65, .96)]\nprint([name for name, win, safe in runs if safe >= safety_floor])', 'gate',
 ['安全门槛 0.95，候选安全 0.85 但胜率更高，怎么办？','判为不可行',['让胜率补分','删除安全指标'],'硬约束不参与加权抵消。']);
add(60, '小补丁省了参数，就值得替换全量微调吗？',
 '这一关把 LoRA 从一个公式变成一次可交付实验。像比较两种做饭方法：食材、份量和评分人相同，才能判断省时省力有没有牺牲成品。',
 [['baseline','用来比较的参照训练方法。'],['trainable parameters','真正参与更新的参数，区别于总参数。'],['validation loss','在独立验证数据上的损失，用来观察泛化。']],
 [['先固定任务','使用相同数据划分、tokenizer、监督区间、训练预算和评测方式。否则方法变化与数据变化会混在一起。'],['先做参数账本','8→6 的线性层全量更新 48 个数，rank=2 的旁路更新 28 个。较大层上节省比例可能更多。'],['再看训练结果','LoRA 更新参数更少，但 forward 仍经过底座。要实测步时、峰值显存和验证效果，保存 adapter 才有可复用产物。']],
 '可训练参数减少 90% 不等于整体显存减少 90%，底座、激活与工作区仍有成本。',
 'full, adapter = 48, 28\nreduction = 1 - adapter/full\nprint(round(reduction*100,1))  # 41.7% trainable parameters', 'lora',
 ['可训练参数变少，可以直接推出整机显存按相同比例变少吗？','不能，还存在底座和激活等开销',['能，所有显存都是可训练参数','能，LoRA 不做前向'],'训练账本至少还包含权重、激活、梯度和优化器状态。']);
add(61, '看一份建筑图纸，能知道多少，又有哪些必须实地测？',
 '模型 config 像图纸。层数、头数、维度帮助你计算理论规模；真实吞吐还取决于 kernel、硬件和调度。先看懂图纸，再提出值得测试的结构变化。',
 [['hidden size','每个 token 表示向量的宽度。'],['head dimension','每个 Attention 头处理的维度。'],['KV heads','保存 Key/Value 的头数；GQA 允许少于 query 头数。']],
 [['核对维度','hidden=16、query heads=4，那么每头维度=4。不能把不整除的配置悄悄向下取整。'],['比较缓存','固定其他条件，KV heads 从 4 减到 2，普通 KV 理论元素数减半。'],['识别边界','修改 config 字段并不自动得到训练好的兼容模型；权重形状、训练和 backend 都需要对应。']],
 '把理论缓存节省直接写成“模型加速两倍”，或给缺失的结构字段填任意默认值。',
 'hidden, heads = 16, 4\nassert hidden % heads == 0\nhead_dim = hidden // heads\nprint(head_dim)  # 4', 'kv',
 ['hidden=16、query heads=4，每头维度是多少？','4',['16','64'],'把表示宽度均分给 4 个头：16/4。']);
add(62, '模型会照着说话，和模型会完成指令，是一回事吗？',
 '指令微调像教学生读题后作答。题干、材料和答案要分清；评测还得看是否按要求回答，不能只背熟训练文本。',
 [['instruction','要完成的任务，例如总结或分类。'],['input','执行任务需要的材料，可能为空。'],['response','期望回答，是监督的主要部分。']],
 [['拼训练样本','把指令和材料放进 prompt，正确答案放进 response，遵循固定模板。'],['核对监督区','如果训练目标是回答，prompt 对应的 labels 通常设 -100，只在回答位置计损失。'],['检查交付','在独立样本上检查内容与格式。例如要求 JSON 时，低 loss 不能代替 JSON 可解析性检查。']],
 '只看 train loss 就宣布可以交付，忽略任务完成率、格式和数据泄漏。',
 'is_response = [False, False, True, True]\ntokens = [10, 20, 30, 40]\nlabels = [t if keep else -100 for t,keep in zip(tokens,is_response)]\nprint(labels)  # [-100, -100, 30, 40]', 'data',
 ['希望只监督回答，prompt 的 label 通常设成什么？','ignore_index（常用 -100）',['原 token id 全部保留','全部设为 token 0'],'忽略标签让 prompt 不直接贡献监督损失，前向仍会读取 prompt。']);
add(63, '两个补丁谁更好，应该只比大小吗？',
 'LoRA 变体比较像同一赛道的试跑。rank 小可能更省，但训练效果不一定更好；需要在相同预算里比较，而不是拿不同训练轮数的结果直接排名。',
 [['controlled variable','除了要比较的方法，其余条件尽量保持相同。'],['parameter efficiency','用较少可训练参数取得任务效果的能力。'],['stability','多次运行或训练过程是否出现明显波动。']],
 [['列候选','比较 rank=2 与 rank=4，固定相同层、缩放约定、数据和步数。'],['计算规模','8→6 的旁路参数分别是 28 与 56，翻倍；不代表效果也翻倍。'],['结合验证','同一质量门槛下再比较步时、显存和失败情况，记录随机种子和变体实现。']],
 '候选 A 训练 10 步、B 训练 100 步，却把效果差异都归因于变体。',
 'for rank in [2, 4]:\n    params = rank * (8 + 6)\n    print(rank, params)  # 2 28; 4 56', 'lora',
 ['其他条件固定，rank 从 2 变 4，LoRA 参数量怎样变？','翻倍',['不变','变成四倍'],'参数量 r×(输入维+输出维)，对 r 是线性关系。']);
add(64, '测试卷混进练习册，分数漂亮还可靠吗？',
 '数据质量项目先决定数据能否进训练。这里特别关注评测泄漏：模型见过测试答案，分数就不能说明面对新问题的能力。',
 [['leakage','评测信息通过训练数据或处理流程提前泄漏。'],['split','训练、验证和测试数据的划分。'],['audit','按明确规则检查数据并记录问题比例。']],
 [['先统一格式','去空白、规范字段，再检查重复，否则外形差异会掩盖重复内容。'],['跨集合检查','训练样本 A、B，评测样本 B、C；交集 B 提示泄漏风险，不能只在训练集内部去重。'],['输出结论','列明空回答、重复、模板问题和覆盖缺口，再判断修复后是否重新划分。']],
 '清洗时查看测试答案并据此设计训练特征，也属于泄漏风险，不只是字符串重复。',
 'train = {"A", "B"}\neval_set = {"B", "C"}\nprint(train & eval_set)  # {B}', 'data',
 ['训练是 A、B，评测是 B、C，应首先检查哪条？','B',['只检查 C','都没有风险'],'B 同时出现在训练和评测中，可能高估泛化表现。']);
add(65, '预算有限时，选全量、LoRA 还是 QLoRA？',
 '把方案放进一张选择表：先看装不装得下，再看质量与吞吐能否过线。QLoRA 的量化底座可能省空间，但额外误差与计算路径也要计入。',
 [['full fine-tuning','更新完整模型参数。'],['LoRA','底座固定，主要训练低秩旁路。'],['QLoRA','低比特底座配合可训练 adapter。']],
 [['写约束','预算 10 GiB、验证质量至少 0.90。'],['筛候选','全量用 16 GiB 不可行；LoRA 12 GiB 也不可行；QLoRA 8 GiB、质量 0.92 才进入比较。'],['保留条件','如果 QLoRA 质量只有 0.85，则没有可行候选。正确结论是调整计划，不是强选一个。']],
 '没有任何候选通过，却总返回排行榜第一名。空的可行集合是重要结果。',
 'runs = [("full",16,.95),("lora",12,.94),("qlora",8,.92)]\nvalid = [r for r in runs if r[1] <= 10 and r[2] >= .90]\nprint(valid)', 'gate',
 ['预算 10 GiB，QLoRA 8 GiB 但质量 0.85<0.90，怎么办？','排除它；可能没有可行候选',['一定选 QLoRA','把质量字段删掉'],'省显存是必要条件之一，不能替代质量门槛。']);
add(66, '第一口菜来得快，和整桌菜上得快，是同一个指标吗？',
 'TTFT 是等第一口，TPOT 是后续每口间隔，E2E 是整桌上完的总等待。吞吐还要看单位时间服务了多少请求，不能只盯一个数字。',
 [['TTFT','从请求开始到首个 token 的时间，需明确起止点。'],['TPOT','后续输出 token 的平均间隔。'],['E2E','请求完整结束的时延。']],
 [['做时间拆分','假设 TTFT=100ms，生成 5 个 token，后续间隔每次 20ms，共 4 段。'],['算结束时间','E2E≈100+4×20=180ms，首 token 已包含在 TTFT 中。'],['再看并发','增加并发可能提高总体吞吐，也可能增加排队。固定输入/输出长度和计时口径才可比较。']],
 '把生成 5 个 token 算成 TTFT+5×TPOT，或者把单请求变快与服务容量提升混为一谈。',
 'ttft_ms, tokens, tpot_ms = 100, 5, 20\ne2e_ms = ttft_ms + max(tokens-1,0)*tpot_ms\nprint(e2e_ms)  # 180', 'latency',
 ['TTFT 100ms，输出 5 token，后续间隔 20ms，E2E 约多少？','180ms',['200ms','120ms'],'后续间隔数是 5−1=4。']);
add(67, '压缩包很小，为什么解压后运行未必更快？',
 '量化部署要验证储存格式、执行内核和输出质量。像压缩行李：体积变小有用，但每次取东西都要额外解包，就可能拖慢旅程。',
 [['artifact','保存的量化权重及必要的配置/scale。'],['dequantization','把量化表示恢复成可参与计算的近似值。'],['kernel support','后端是否真的有适配该低比特表示的执行路径。']],
 [['先跑量化闭环','浮点→整数→恢复浮点，比较恢复误差和理论字节数。'],['分清两种路径','低比特 kernel 直接使用压缩表示，与先完整反量化再普通矩阵乘，访存和开销可能不同。'],['形成部署证据','同一 workload 测质量、TTFT、TPOT、吞吐和峰值显存；记录实际加载的格式与后端。']],
 '文件小就声明推理快，或者用模拟整数舍入证明硬件 INT4 kernel 已生效。',
 'baseline_ms = 10\nlowbit_compute_ms, unpack_ms = 6, 5\ncandidate_ms = lowbit_compute_ms + unpack_ms\nprint(candidate_ms < baseline_ms)  # False', 'quant',
 ['原来 10ms，低比特计算 6ms、额外解包 5ms，总体是否更快？','没有，总计 11ms',['更快，因为计算只要 6ms','快一倍'],'端到端要把新增开销也加进去。']);
add(68, '速记员写得快，审稿人忙不过来，最终能省时间吗？',
 '投机解码 benchmark 比较的是完整 draft+verify 路径。接受率高只是其中一项，草稿成本、验证成本和回退都影响收益。',
 [['acceptance rate','按约定分母计算的候选接受比例。'],['draft cost','生成草稿需要的时间或计算量。'],['verify cost','目标模型验证草稿的成本。']],
 [['算基线','假设普通解码每 token 花 10 个时间单位。'],['算一轮','草稿+验证合计 30 单位，最后推进 2 token，平均 15/token，反而慢；推进 4 token 才是 7.5/token。'],['确认真实路径','检查后端确实启用 speculative。普通 target-only 服务可以做 baseline，不能算已开启投机。']],
 '把 draft 提出长度当作实际推进长度，或用接受率替代最终吞吐测量。',
 'round_cost, progress = 30, 2\nper_token = round_cost / progress\nprint(per_token)  # 15 > baseline 10', 'speculate',
 ['一轮成本 30，只推进 2 token，平均成本是多少？','15/token',['2/token','30/4=7.5/token'],'分母必须是实际推进的 2，而不是提议的长度。']);
add(69, '仓库里存满旧开场白，真的有人会再用吗？',
 '前缀缓存 benchmark 把请求分布带回决策。每个请求都不重复时，再精巧的缓存也很难省掉 prefill；高命中还可能伴随管理成本。',
 [['hit tokens','从缓存复用的 token 数。'],['reuse ratio','复用 token 数占总 prompt token 的比例。'],['maintenance','缓存查找、淘汰和状态管理的成本。']],
 [['数复用','10 个 prompt token 命中 6 个，复用比例是 60%，还有 4 个必须计算。'],['换请求模式','重复相同前缀与全部独特前缀，是不同 workload，不能把两者性能直接归因于实现。'],['同场景对照','关闭/开启缓存时使用同一请求流，比较 TTFT、命中、容量和维护开销。']],
 '用完全重复的理想请求流跑出高命中率，就推断所有业务都值得部署。',
 'prompt_tokens, hit_tokens = 10, 6\nprint(hit_tokens/prompt_tokens)  # 0.6\nprint(prompt_tokens-hit_tokens)  # 4 still need prefill', 'prefix',
 ['10 个输入 token 命中 6 个，还有多少需要新 prefill？','4',['0','6'],'命中只节省前缀，不会自动处理剩余后缀。']);
add(70, '餐厅接单更多了，但最慢顾客等到生气，算成功吗？',
 '调度器 benchmark 既看吞吐，也看排队与尾延迟。平均等待可以掩盖少数特别慢的请求，服务目标必须一起检查。',
 [['arrival','请求到达时刻。'],['tail latency','高分位时延，用于观察较慢的一部分请求。'],['SLO','明确的服务目标，例如延迟上限。']],
 [['建请求流','先固定到达时间、输入输出长度和并发条件，保存每个请求的完成记录。'],['看平均与最慢','等待为 1、1、1、17，平均只有 5，但仍有人等了 17。小样本的“最慢”也不应随意称为 P99。'],['做约束选择','候选吞吐上升但超出规定尾延迟门槛，应调参或拒绝，不能只报好看的指标。']],
 '把平均延迟通过说成所有请求都通过，或在不同到达流下比较调度算法。',
 'waits = [1, 1, 1, 17]\nprint(sum(waits)/len(waits), max(waits))  # 5, 17', 'schedule',
 ['等待 1、1、1、17，平均值与最大值分别是多少？','5 和 17',['4 和 17','5 和 5'],'平均会被短请求拉低，不能代表慢请求。']);
add(71, '每个读书小组都记一份笔记，能不能共享，甚至换一种记法？',
 'MHA 每个头有自己的 K/V，GQA 让一组 query 头共享 K/V，MLA 改变缓存表示，保存压缩潜变量与必要位置分量。三者要用同一账本比较。',
 [['head','并行观察表示的一个注意力子空间。'],['latent','较低维的中间表示，需要模型结构配合。'],['bytes','元素数乘每元素字节数，是理论数据区大小。']],
 [['从一层算起','batch=1，长度=8，KV heads=4，head dim=4，FP16：K/V 合计 2×1×8×4×4×2=512 字节。'],['试试共享','KV heads 从 4 改为 2，其他不变，GQA 账本变成 256 字节。query heads 不直接替代 KV heads。'],['换表示','课堂 MLA 设 latent=8、位置分量=4：1×8×(8+4)×2=192 字节。真实布局依赖模型实现，不能仅改字段就转换结构。']],
 '用 query heads 代替 KV heads，忘记 K 和 V 两份，或把 192 字节的简化账本当真实显存实测。',
 'batch, tokens, kv_heads, dim, bytes_per = 1, 8, 4, 4, 2\nkv_bytes = 2*batch*tokens*kv_heads*dim*bytes_per\nprint(kv_bytes)  # 512 per layer', 'kv',
 ['上述 512 字节账本中，KV heads 减半后是多少？','256 字节',['128 字节','512 字节'],'其他维度固定时，普通 KV 数据区对 KV heads 线性变化。']);
add(73, '显存账单为什么不止模型权重一项？',
 '训练的一步要同时保留很多东西。像工坊除了原料，还有半成品、修改记录和工具。先把参数、梯度、优化器状态逐项记清楚，再观察激活随 batch 与长度增长。',
 [['gradient','每个可训练参数对应的更新方向信息。'],['optimizer state','优化器持有的额外状态，如 Adam 的两个矩。'],['peak allocated','运行时分配峰值，区别于理论张量账本和预留内存。']],
 [['最小例子','4 个 FP32 参数：权重 16 字节、梯度 16 字节。'],['加 Adam 状态','两个 FP32 矩各 16 字节，合计再加 32；现在共 64 字节，还没有激活。'],['连接测量','optimizer state 常在第一次更新时创建。只在初始化后检查可能漏项；真实峰值还取决于中间张量生命周期。']],
 '把初始化时的显存当完整训练峰值，或把 allocated、reserved、设备总占用当成同一指标。',
 'n_params, bytes_per = 4, 4\nweights = gradients = n_params*bytes_per\nadam_states = 2*n_params*bytes_per\nprint(weights+gradients+adam_states)  # 64', 'memory',
 ['4 个 FP32 参数，权重+梯度+Adam 两个 FP32 矩，共多少字节？','64',['16','32'],'四份状态，每份 4×4=16 字节。']);
add(74, '把只占 10% 的步骤加速十倍，整件事会快十倍吗？',
 'Profiling 先找到时间花在哪里，再决定改哪里。Amdahl 定律提醒我们：没改动的部分会限制总收益。',
 [['profile','按阶段/事件观察时间和资源消耗。'],['critical path','依赖链上决定总完成时间的部分。'],['Amdahl','局部加速受到未优化部分的限制。']],
 [['拆 100 秒','某步骤占 20 秒，其余 80 秒。'],['局部快四倍','20 秒缩成 5 秒，总计 85 秒，总加速是 100/85≈1.18 倍。'],['验证归因','先用 trace 形成假设，再固定 workload 重测端到端。缓存、warm-up 或测量方式变化也可能造成差异。']],
 '把 kernel 自身的加速倍数直接写成整个模型的加速倍数。还有未优化部分与额外开销。',
 'fraction, local_speedup = .2, 4\ntotal_speedup = 1 / ((1-fraction)+fraction/local_speedup)\nprint(round(total_speedup,2))  # 1.18', 'amdahl',
 ['原总耗时 100 秒，其中 20 秒快四倍后，总耗时多少？','85 秒',['25 秒','80 秒'],'其余 80 不变，优化部分变成 5。']);
add(75, '省下显存，却让速度降到不能用，这笔交易划算吗？',
 '预算决策像筛选租房方案：面积合适还不够，租金、通勤和基本条件要同时过线。这一关读取已有测量报告做决策，不凭空创造性能数据。',
 [['budget','可使用的资源上限。'],['retention','优化后吞吐占 baseline 吞吐的比例。'],['feasible set','通过全部硬门槛的候选集合。']],
 [['读报告','baseline 100 tokens/s，候选 70 tokens/s，吞吐保留率 70%。'],['先过门槛','如果最低保留 80%，即使显存省很多，这个候选仍不可行。'],['再看收益','通过质量与吞吐门槛后，才比较显存节省与剩余空间。缺字段时报告不完整，不能默认为通过。']],
 '把缺失吞吐当 0 后继续排名，或者把未知质量当作质量通过。',
 'baseline_tps, candidate_tps = 100, 70\nretention = candidate_tps/baseline_tps\nprint(retention >= .80)  # False', 'gate',
 ['baseline 100，候选 70 tokens/s，最低保留率 80%，能通过吗？','不能，只有 70%',['能，省了显存','能，70 比 0 大'],'优化约束必须同时满足。']);
add(76, '少记笔记、重新推导、去仓库取，哪种方式更适合这次任务？',
 'Checkpoint 用重算换存储，offload 用搬运换存储。不同 workload 的重算量和传输能力不同，不能预先宣布一种方法总赢。',
 [['checkpoint','保存少量检查点，反向时重建需要的激活。'],['offload','暂存到主机，反向前取回。'],['combination','在不同区域搭配策略，收益与开销不一定简单相加。']],
 [['同一基线','固定模型、batch、长度、dtype 和质量口径，分别测试三种候选。'],['课堂比较','若重算代价 1.5 秒，8 GiB 往返、带宽 8 GiB/s 的搬运代价 2 秒，时间上重算较少。'],['检查实际峰值','策略可能引入临时缓冲，节省必须按真实峰值看。能重叠多少也需要时间线证据。']],
 '不同策略使用不同长度或 batch，然后把更低显存都归于策略；或者把 CPU 模拟当 GPU 实测。',
 'recompute_s = 1.5\noffload_s = 2*8/8\nprint(min(recompute_s, offload_s))  # 1.5, serial toy model', 'offload',
 ['重算 1.5 秒，串行搬运 2 秒，只比较这部分时间谁更少？','重算',['搬运','两者永远一样'],'仅在这个固定条件下成立，带宽和重叠变化可能反转结论。']);
add(79, '流水线越长，工人越多，为什么还会有人闲着？',
 '把模型切成阶段后，第一批数据要逐段进入，最后一批要逐段退出。更多 microbatch 可以摊薄这段暖机/排空时间，但不能无限忽略调度成本。',
 [['stage','流水线中的一个计算阶段。'],['microbatch','一个 batch 拆出的较小工作单元。'],['bubble','设备没有有效计算的空闲槽位。']],
 [['设简化条件','仅看前向，3 个等时长阶段，每阶段处理一个 microbatch 需 1 个单位。'],['算总槽位','4 个 microbatch 的总时间为 m+p−1=4+3−1=6。'],['看利用率','有效工作量 3×4=12；可用槽位 3×6=18，利用率 2/3。训练的反向与 1F1B 调度需要另一套账本。']],
 '把前向玩具流水线公式直接用于任意训练调度，或忽略阶段之间不均衡。',
 'stages, microbatches = 3, 4\nticks = microbatches+stages-1\nutilization = microbatches/ticks\nprint(ticks, round(utilization,2))  # 6, 0.67', 'bubble',
 ['3 个等时前向阶段，4 个 microbatch，总共多少时间单位？','6',['4','12'],'第一批穿过 3 阶段，后续每单位完成一批：3+3=6。']);
add(80, '专家都分到不同设备了，吞吐为什么还是没有起来？',
 '专家并行 benchmark 要把路由、负载和通信一起放到同一张报告里。速度慢可能发生在“把人送到窗口”的路上，也可能发生在某个窗口拥堵。',
 [['imbalance','最忙与平均负载之间的差异。'],['capacity','专家能接收的容量，溢出需要明确定义处理。'],['dispatch/combine','发送 token 到专家，再汇总输出的过程。']],
 [['看负载','两个专家分到 7 与 1 份工作，即使总量只有 8，最忙的仍要做 7。'],['算理想效率','同速、无通信下，有效工作/总槽位=8/(2×7)≈57.1%。'],['加入网络','再增加 all-to-all、合并与同步成本，真实效率只会受更多因素影响。'],],
 '只看总 token 数或平均负载，漏掉 capacity 溢出和最慢 rank。',
 'loads = [7,1]\nefficiency = sum(loads)/(len(loads)*max(loads))\nprint(round(efficiency,3))  # .571', 'parallel',
 ['两个同速专家负载 7 与 1，无通信时利用率约多少？','57.1%',['100%','87.5%'],'8 份有效工作除以 14 个总槽位。']);
add(81, '开两家店，客人真的会平均分过去吗？',
 '分布式推理逻辑验证像模拟分店接单。路由规则、请求大小和通信决定负载；单机模拟能验证规则，不能测出真实多卡网络速度。',
 [['replica','能独立服务请求的模型副本。'],['routing','把请求分配给实例的策略。'],['makespan','这批请求全部完成所需的时间。']],
 [['相同请求集合','工作量为 6、2、1、1，分给两个同速实例。'],['比较分配','若负载为 8 与 2，总完成时间为 8；若能做到 6 与 4，完成时间为 6。'],['再看可执行性','负载最均匀的离线方案未必在线可得；实际路由还受到到达时刻、缓存和实例状态约束。']],
 '把单机模拟的工作量单位写成真实毫秒，或者用逻辑通过宣布平台部署完成。',
 'for loads in [[8,2], [6,4]]:\n    print(max(loads))  # 8, 6 (work units)', 'parallel',
 ['两个同速实例负载 6 与 4，整批完成时间由哪个决定？','6',['4','平均 5'],'必须等最忙实例完成，所有请求才算完成。']);
add(84, '模型更喜欢正确答案了吗？先把“喜欢”的刻度说清楚。',
 'DPO 使用成对偏好比较 policy 与 reference 的相对倾向。项目评估先检查偏好对，再把 margin、loss 和有效样本比例放在一起看。',
 [['log probability','概率的对数；序列常通过 token log probability 累加。'],['margin','chosen 与 rejected 的相对分数差。'],['reference adjustment','减去参照模型的相对偏好，避免只看 policy 绝对值。']],
 [['算 policy 差','chosen=-2，rejected=-4，policy margin=2。'],['扣掉 reference','reference margin=1，调整后 margin=1；beta=1 时 sigmoid(1)≈0.731。'],['理解 loss','-log(0.731)≈0.313。这个数小表示这对样本偏好拟合较好，不等同整体任务质量已经提升。']],
 '把负的 log probability 看成“不合法概率”，或只看训练 pair 的 loss 就下交付结论。',
 'import math\npolicy_margin, reference_margin, beta = 2, 1, 1\nz = beta*(policy_margin-reference_margin)\nloss = math.log1p(math.exp(-z))\nprint(round(loss,3))  # .313', 'dpo',
 ['policy margin=2，reference margin=1，调整后的 margin 是多少？','1',['3','2'],'先做 policy−reference，再乘 beta。']);
add(85, '一个小组都考 90 分，谁应该得到更多鼓励？',
 'GRPO 的相对奖励在同组内比较。大家分数相同时，没有谁比组平均更好；组间整体高低不能直接替代组内差异。',
 [['group','同一个 prompt 或约定条件下的一组候选。'],['advantage','相对组均值的标准化奖励。'],['epsilon','防止除以零的小量，不会凭空制造优势。']],
 [['取简单奖励','奖励 1、2、3，均值 2。使用总体标准差时 std≈0.816。'],['计算优势','(reward−mean)/(std+eps)≈−1.225、0、1.225。中间样本等于均值，优势为 0。'],['考虑退化组','若奖励为 2、2、2，分子全部为 0，加 eps 后仍是 0。还要观察组覆盖、波动与最差组。']],
 '不同组混在一起标准化，或给零方差组强行制造随机优势。',
 'rewards = [1,2,3]\nmean = sum(rewards)/len(rewards)\nstd = (sum((r-mean)**2 for r in rewards)/len(rewards))**.5\nprint([(r-mean)/(std+1e-8) for r in rewards])', 'grpo',
 ['奖励 1、2、3，奖励为 2 的候选在组内优势是多少？','0',['1','2'],'它等于组均值，标准化分子是 0。']);
add(86, '线上胜率涨了，可更新越来越慢，这条改进能持续吗？',
 '在线 DPO benchmark 观察持续反馈与更新的完整循环。它像长期运营一间店：一次好评高峰不代表可以忽略质量下限和更新成本。',
 [['window','同一次统计使用的反馈与评测区间。'],['update latency','从反馈准备到新策略可用的时间，需固定口径。'],['release gate','决定可上线、继续观察还是暂停的规则。']],
 [['固定比较','baseline 和 candidate 使用相同反馈流、初始模型与评测窗口。'],['列硬门槛','候选胜率 0.65，比 0.60 好，但安全分 0.92 低于 0.95，仍不能通过。'],['检查持续性','满足安全后，再看更新时延、稳定性和重复窗口是否保持收益；回退路径也应清楚。']],
 '把一次模拟门槛通过当成真实线上验收；逻辑测试只证明决策函数在给定输入下的行为。',
 'candidate = {"win":.65, "safety":.92, "update_ms":120}\npass_gate = candidate["safety"] >= .95 and candidate["update_ms"] <= 100\nprint(pass_gate)  # False', 'gate',
 ['胜率提高，但安全 0.92<0.95，更新 120ms>100ms，能通过吗？','不能，两项门槛都未满足',['能，胜率提高即可','只把结果保存就算上线'],'更高胜率不等于满足发布条件。']);
module.exports = plans;
